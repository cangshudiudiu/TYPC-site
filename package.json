{
  "name": "typc-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/create-worker-entry.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.1",
    "astro": "^5.8.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.93.1"
  }
}
